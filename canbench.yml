build_cmd: cargo build --release --target wasm32-unknown-unknown --features canbench-rs

wasm_path: ./target/wasm32-unknown-unknown/release/gate_service.wasm

init_args:
  # This is Candid encoded hex for initialization args
  # It is retrieved by using the following command:
  # echo '(record { log_settings = null; owner = principal "ryjl3-tyaaa-aaaaa-aaaba-cai"; permissions = null })' | didc encode --format hex
  hex: 4449444c016c0384f9d1767fb3b0dac30368bedef1ef0c7f0100010a00000000000000020101
